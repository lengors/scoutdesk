---
server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: scoutdesk
  threads:
    virtual:
      enabled: true
  security:
    role:
      mappings:
        admin:
          - "Demo Admins"
        developer:
          - "Demo Developers"
        user:
          - "Demo Users"